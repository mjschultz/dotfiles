GNOME_OPEN=$(which gnome-open 2> /dev/null)
EVINCE_OPEN=$(which evince 2> /dev/null)

if [ -n "$GNOME_OPEN" ] ; then
    alias open=$GNOME_OPEN
elif [ -n "$EVINCE_OPEN" ] ; then
    alias open=$EVINCE_OPEN
fi
